Organization
1. Change the current index.html to CEA(Subject code)-COA(Book code).html
2. Move all of the current sub .html files to its own folder ../CEA/COA/
3. A new index.html links to more subjects 
|
New architecture
    index.html (New) -> CEA.html (Old index.html) -> <Chapters>.html


Future Proofing 
1. Create two or three templates/placeholders (With nothing in them yet) with different designs and color pallates for more subjects (Cal, Algebra, ...)
2. In the new index.html create sections (right lets only made one for CEA and one for MAE)
    | MAE section Contains Cal 1, Cal 2, Algebra

QOL
1. In existing files of COA add explaination for keywords (When hover over certain words a small window pop up next to cursor with the explaination of that word)
2. Remake the Chapter 17 UI so that it matches the others (But keep the current version of it. Add a button within only chapter 17 that allow to switch back and front the old design and the new design(Only for chapter 17 of COA dont do this to any other chapters))
